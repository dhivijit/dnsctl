# Core (CLI + TUI)
requests>=2.31
cryptography>=41.0
keyring>=24.0
click>=8.1
GitPython>=3.1
textual>=0.60

# GUI extra — install with: pip install dnsctl-app[gui]
# PyQt6>=6.6
# qt-material>=2.14
# qtawesome>=1.3
